# RSL-DCE-control-program
We start with a command line DCE control program that pass the name of the satellite pass file as a command line parameter, parse the text, then send command to hardware.  The satellite pass file contains fields that affect the strength and quality of the RF link, and the file is generated using MATLAB toolboxes.
